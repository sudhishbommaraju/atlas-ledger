# Atlas Ledger — Frontend

React + TypeScript + Vite + Tailwind CSS.

## Quick Start

```bash
npm install
npm run dev
```

Runs at http://localhost:3000. Requires backend at http://localhost:8000.

## Environment

Set `VITE_API_BASE_URL` to override the backend URL (default: `http://localhost:8000`).
